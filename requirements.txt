# Pinned to a floor + major-version ceiling for reproducible deploys; run pip-compile (or
# equivalent) periodically to refresh and get an exact lockfile.
boto3>=1.34,<2.0
requests>=2.31,<3.0
pyyaml>=6.0,<7.0
